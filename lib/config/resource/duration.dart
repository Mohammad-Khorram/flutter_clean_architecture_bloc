class DurationConfig {
  // general
  static Duration widgetAnimation = const Duration(milliseconds: 400);

  // millisecond
  static Duration milliseconds100 = const Duration(milliseconds: 100);
  static Duration milliseconds200 = const Duration(milliseconds: 200);

  // second
  static Duration second1 = const Duration(seconds: 1);
  static Duration seconds2 = const Duration(seconds: 2);
}
